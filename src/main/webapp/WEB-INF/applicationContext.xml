<beans:beans xmlns="http://www.springframework.org/schema/beans" xmlns:sec="http://www.springframework.org/schema/security"
   xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
   xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

   <sec:http use-expressions="true">
      <sec:intercept-url pattern="/admin/**" access="hasRole('admin')" />
      <sec:intercept-url pattern="/**" access="permitAll" />
      <sec:http-basic />
   </sec:http>

   <sec:authentication-manager>
      <sec:authentication-provider>
         <sec:password-encoder hash="{sha}" />
         <sec:user-service>
            <sec:user name="admin" password="{SHA}U2s/mJ0DnOGPWWOYYZoJoTrAV/Q=" authorities="user, admin" />
         </sec:user-service>
      </sec:authentication-provider>
   </sec:authentication-manager>

   <bean id="reCaptcha" class="net.tanesha.recaptcha.ReCaptchaImpl">
      <property name="privateKey" value="${recaptcha.privateKey}" />
      <property name="publicKey" value="${recaptcha.publicKey}" />
      <property name="includeNoscript" value="false" />
   </bean>
   
   <!-- JSP EL variables -->
   <bean class="org.springframework.web.context.support.ServletContextAttributeExporter">
      <property name="attributes">
         <map>
            <entry key="cdnUrl" value="#{ systemProperties['cdn.url'] == null ? '' : systemProperties['cdn.url'] }" />
         </map>
      </property>
   </bean>

   <beans profile="default">
      <context:property-placeholder location="classpath:environment-localhost.properties"
         system-properties-mode="OVERRIDE" />
   </beans>
   <beans profile="prod">
      <context:property-placeholder system-properties-mode="OVERRIDE" />
   </beans>
</beans:beans>